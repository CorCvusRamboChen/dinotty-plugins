#!/usr/bin/env node
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// src/claude.ts
var import_node_child_process = require("node:child_process");
var import_node_util = require("node:util");
var fs = __toESM(require("node:fs"));
var path = __toESM(require("node:path"));
var os = __toESM(require("node:os"));
var execFileAsync = (0, import_node_util.promisify)(import_node_child_process.execFile);
var HOME = os.homedir();
var IS_WINDOWS = process.platform === "win32";
var MIN_CLAUDE_VERSION = "2.0.0";
var CAPABILITIES_SINCE = "2.1.205";
var OVERRIDE_ENV = "CLAUDE_REMOTE_CLAUDE_BIN";
async function locateClaude() {
  const override = process.env[OVERRIDE_ENV];
  if (override && isExecutableFile(override)) {
    return { bin: override, source: "override" };
  }
  const fromPath = await lookupOnPath();
  if (fromPath) return { bin: fromPath, source: "path" };
  for (const candidate of knownLocations()) {
    if (isExecutableFile(candidate)) return { bin: candidate, source: "known-location" };
  }
  return null;
}
async function lookupOnPath() {
  const finder = IS_WINDOWS ? "where.exe" : "which";
  try {
    const { stdout } = await execFileAsync(finder, ["claude"], { timeout: 5e3 });
    const hits = stdout.split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
    if (!hits.length) return null;
    if (!IS_WINDOWS) return hits[0];
    const preferred = [".cmd", ".bat", ".exe", ".ps1"];
    for (const ext of preferred) {
      const hit = hits.find((h) => h.toLowerCase().endsWith(ext));
      if (hit) return hit;
    }
    return hits.find((h) => path.extname(h)) ?? null;
  } catch {
    return null;
  }
}
function knownLocations() {
  const out = [];
  if (IS_WINDOWS) {
    const appData = process.env.APPDATA;
    if (appData) out.push(path.join(appData, "npm", "claude.cmd"));
    const localAppData = process.env.LOCALAPPDATA;
    if (localAppData) out.push(path.join(localAppData, "Programs", "claude", "claude.exe"));
  } else {
    out.push(path.join(HOME, ".claude", "local", "claude"));
    out.push("/usr/local/bin/claude");
    out.push("/opt/homebrew/bin/claude");
    const nvmVersions = path.join(HOME, ".nvm", "versions", "node");
    const defaultAlias = path.join(HOME, ".nvm", "alias", "default");
    try {
      const version = fs.readFileSync(defaultAlias, "utf-8").trim();
      if (version) out.push(path.join(nvmVersions, version, "bin", "claude"));
    } catch {
    }
    try {
      for (const v of fs.readdirSync(nvmVersions).sort().reverse()) {
        out.push(path.join(nvmVersions, v, "bin", "claude"));
      }
    } catch {
    }
  }
  return out;
}
function isExecutableFile(candidate) {
  try {
    return fs.statSync(candidate).isFile();
  } catch {
    return false;
  }
}
async function probeClaude() {
  const located = await locateClaude();
  if (!located) {
    return { found: false, minVersion: MIN_CLAUDE_VERSION, error: "claude executable not found" };
  }
  try {
    const { stdout } = await runClaude(located.bin, ["--version"], { timeout: 1e4 });
    const version = stdout.trim().split(/\s+/)[0] ?? "";
    return {
      found: true,
      bin: located.bin,
      source: located.source,
      version,
      versionOk: compareVersions(version, MIN_CLAUDE_VERSION) >= 0,
      reportsCapabilities: compareVersions(version, CAPABILITIES_SINCE) >= 0,
      minVersion: MIN_CLAUDE_VERSION
    };
  } catch (e) {
    return {
      found: true,
      bin: located.bin,
      source: located.source,
      minVersion: MIN_CLAUDE_VERSION,
      error: e instanceof Error ? e.message : String(e)
    };
  }
}
function runClaude(bin, args, opts) {
  if (IS_WINDOWS && isBatchFile(bin)) {
    const comspec = process.env.ComSpec || "cmd.exe";
    return execFileAsync(comspec, ["/d", "/s", "/c", bin, ...args], opts);
  }
  return execFileAsync(bin, args, opts);
}
function isBatchFile(bin) {
  const ext = path.extname(bin).toLowerCase();
  return ext === ".cmd" || ext === ".bat";
}
function spawnTurn(bin, prompt, opts = {}) {
  const args = ["-p", "--output-format", "stream-json", "--verbose"];
  if (opts.partialMessages) args.push("--include-partial-messages");
  if (opts.resumeSessionId) args.push("--resume", opts.resumeSessionId);
  if (opts.permissionMode) args.push("--permission-mode", opts.permissionMode);
  if (opts.allowedTools?.length) args.push("--allowedTools", opts.allowedTools.join(","));
  if (opts.model) args.push("--model", opts.model);
  const child = IS_WINDOWS && isBatchFile(bin) ? (0, import_node_child_process.spawn)(process.env.ComSpec || "cmd.exe", ["/d", "/s", "/c", bin, ...args], {
    cwd: opts.cwd,
    stdio: ["pipe", "pipe", "pipe"]
  }) : (0, import_node_child_process.spawn)(bin, args, { cwd: opts.cwd, stdio: ["pipe", "pipe", "pipe"] });
  child.stdin?.end(prompt);
  return child;
}
function interruptTurn(child) {
  if (IS_WINDOWS) {
    child.kill();
    return "killed";
  }
  child.kill("SIGINT");
  return "interrupted";
}
function compareVersions(a, b) {
  const pa = a.split(".").map((n) => parseInt(n, 10) || 0);
  const pb = b.split(".").map((n) => parseInt(n, 10) || 0);
  for (let i = 0; i < Math.max(pa.length, pb.length); i++) {
    const diff = (pa[i] ?? 0) - (pb[i] ?? 0);
    if (diff !== 0) return diff;
  }
  return 0;
}

// src/turn.ts
var fs2 = __toESM(require("node:fs"));
var path2 = __toESM(require("node:path"));
var readline = __toESM(require("node:readline"));

// src/protocol.ts
function isInit(e) {
  return e.type === "system" && e.subtype === "init";
}
function isResult(e) {
  return e.type === "result";
}
function textDelta(e) {
  if (e.type !== "stream_event") return null;
  const delta = e.event?.delta;
  return delta?.type === "text_delta" && typeof delta.text === "string" ? delta.text : null;
}
function assistantText(e) {
  if (e.type !== "assistant") return null;
  const parts = e.message?.content;
  if (!Array.isArray(parts)) return null;
  const text = parts.filter((p) => p?.type === "text" && typeof p.text === "string").map((p) => p.text).join("");
  return text || null;
}

// src/reduce.ts
function createTurnReducer(turnId, prompt, now = Date.now) {
  const messages = [{ role: "user", text: prompt }];
  let sessionId = null;
  let model = null;
  let costUsd = null;
  let status = "running";
  let exitCode;
  let revision = 0;
  let updatedAt = now();
  function touch() {
    revision += 1;
    updatedAt = now();
  }
  function currentAssistant() {
    const last = messages[messages.length - 1];
    if (last?.role === "assistant" && last.streaming) return last;
    const created = { role: "assistant", text: "", streaming: true };
    messages.push(created);
    return created;
  }
  function settleStreaming() {
    const last = messages[messages.length - 1];
    if (last?.role === "assistant") delete last.streaming;
  }
  const reducer = {
    get revision() {
      return revision;
    },
    apply(event) {
      if (isInit(event)) {
        sessionId = event.session_id;
        if (event.model) model = event.model;
        touch();
        return;
      }
      const delta = textDelta(event);
      if (delta) {
        currentAssistant().text += delta;
        touch();
        return;
      }
      const assembled = assistantText(event);
      if (assembled !== null) {
        currentAssistant().text = assembled;
        touch();
        return;
      }
      if (isResult(event)) {
        settleStreaming();
        if (typeof event.total_cost_usd === "number") costUsd = event.total_cost_usd;
        if (event.is_error) {
          const detail = event.result || "Claude Code reported an error";
          const suffix = event.api_error_status ? ` (HTTP ${event.api_error_status})` : "";
          const last = messages[messages.length - 1];
          if (last?.role === "assistant" && last.text === detail) {
            messages[messages.length - 1] = { role: "error", text: `${detail}${suffix}` };
          } else {
            messages.push({ role: "error", text: `${detail}${suffix}` });
          }
        } else if (event.result && !lastAssistantHasText()) {
          messages.push({ role: "assistant", text: event.result });
        }
        touch();
        return;
      }
      if (event.type === "sidecar") {
        const sidecar = event;
        if (sidecar.event === "error") {
          messages.push({ role: "error", text: sidecar.error ?? "sidecar error" });
          touch();
        } else if (sidecar.event === "stderr" && sidecar.data) {
          messages.push({ role: "system", text: sidecar.data });
          touch();
        } else if (sidecar.event === "stopping") {
          messages.push({ role: "system", text: "Interrupted." });
          touch();
        }
      }
    },
    finish(code) {
      settleStreaming();
      exitCode = code;
      status = code === 0 ? "done" : "failed";
      if (code !== 0 && !messages.some((m) => m.role === "error")) {
        messages.push({ role: "error", text: `Claude Code exited with code ${code}` });
      }
      touch();
    },
    snapshot() {
      return {
        turnId,
        status,
        sessionId,
        model,
        costUsd,
        exitCode,
        updatedAt,
        // Deep enough: messages are flat records, and the consumer only reads.
        messages: messages.map((m) => ({ ...m }))
      };
    }
  };
  function lastAssistantHasText() {
    for (let i = messages.length - 1; i >= 0; i--) {
      if (messages[i].role === "assistant") return Boolean(messages[i].text);
    }
    return false;
  }
  return reducer;
}

// src/turn.ts
var FLUSH_INTERVAL_MS = 150;
function emit(value) {
  process.stdout.write(JSON.stringify(value) + "\n");
}
function isSafeKey(key) {
  return Boolean(key) && !key.includes("/") && !key.includes("\\") && key !== "." && key !== "..";
}
function readRequest(key) {
  const dataDir = process.env.DINOTTY_PLUGIN_DATA_DIR;
  if (!dataDir) return { error: "DINOTTY_PLUGIN_DATA_DIR is not set" };
  if (!isSafeKey(key)) return { error: `unsafe turn key: ${key}` };
  const file = path2.join(dataDir, `${key}.json`);
  let raw;
  try {
    raw = fs2.readFileSync(file, "utf-8");
  } catch (e) {
    return { error: `cannot read staged turn ${key}: ${e instanceof Error ? e.message : e}` };
  } finally {
    try {
      fs2.unlinkSync(file);
    } catch {
    }
  }
  try {
    const parsed = JSON.parse(raw);
    if (typeof parsed?.prompt !== "string" || !parsed.prompt) {
      return { error: "staged turn has no prompt" };
    }
    return parsed;
  } catch (e) {
    return { error: `staged turn is not valid JSON: ${e instanceof Error ? e.message : e}` };
  }
}
function createSnapshotWriter(turnId, dataDir) {
  const target = path2.join(dataDir, `${turnId}-log.json`);
  const staging = `${target}.tmp`;
  let lastRevision = -1;
  let timer = null;
  function writeNow(reducer) {
    if (reducer.revision === lastRevision) return;
    lastRevision = reducer.revision;
    try {
      fs2.writeFileSync(staging, JSON.stringify(reducer.snapshot()), "utf-8");
      fs2.renameSync(staging, target);
    } catch (e) {
      emit({ type: "sidecar", event: "error", error: `snapshot write failed: ${describe(e)}` });
    }
  }
  return {
    /** Coalescing flush: at most one write per interval while tokens stream. */
    schedule(reducer) {
      if (timer) return;
      timer = setTimeout(() => {
        timer = null;
        writeNow(reducer);
      }, FLUSH_INTERVAL_MS);
      timer.unref?.();
    },
    flush(reducer) {
      if (timer) {
        clearTimeout(timer);
        timer = null;
      }
      writeNow(reducer);
    }
  };
}
function forwardLines(stream, onLine) {
  return new Promise((resolve) => {
    let buffer = "";
    stream.setEncoding("utf-8");
    stream.on("data", (chunk) => {
      buffer += chunk;
      let index;
      while ((index = buffer.indexOf("\n")) !== -1) {
        const line = buffer.slice(0, index).replace(/\r$/, "");
        buffer = buffer.slice(index + 1);
        if (line) onLine(line);
      }
    });
    stream.on("end", () => {
      const tail = buffer.replace(/\r$/, "");
      if (tail) onLine(tail);
      resolve();
    });
    stream.on("error", () => resolve());
  });
}
function installStopHandlers(child, stdinLease, onStop) {
  if (!stdinLease) return () => {
  };
  let stopped = false;
  const stop = (reason) => {
    if (stopped) return;
    stopped = true;
    onStop(reason, interruptTurn(child));
  };
  const rl = readline.createInterface({ input: process.stdin });
  rl.on("line", (line) => {
    if (!line.trim()) return;
    try {
      const msg = JSON.parse(line);
      if (msg?.type === "shutdown" || msg?.type === "interrupt") stop(msg.type);
    } catch {
    }
  });
  rl.on("close", () => stop("stdin-closed"));
  return () => rl.close();
}
async function runTurn(key, options) {
  const dataDir = process.env.DINOTTY_PLUGIN_DATA_DIR ?? "";
  const persist = options.persist && Boolean(dataDir);
  const request = readRequest(key);
  if ("error" in request) {
    const failure = { type: "sidecar", event: "error", error: request.error };
    emit(failure);
    if (persist) {
      const reducer2 = createTurnReducer(key, "(prompt unavailable)");
      reducer2.apply(failure);
      reducer2.finish(1);
      createSnapshotWriter(key, dataDir).flush(reducer2);
    }
    return 1;
  }
  const reducer = createTurnReducer(key, request.prompt);
  const writer = persist ? createSnapshotWriter(key, dataDir) : null;
  const record = (event) => {
    reducer.apply(event);
    writer?.schedule(reducer);
  };
  const located = await locateClaude();
  if (!located) {
    const failure = { type: "sidecar", event: "error", error: "claude executable not found" };
    emit(failure);
    reducer.apply(failure);
    reducer.finish(1);
    writer?.flush(reducer);
    return 1;
  }
  const child = spawnTurn(located.bin, request.prompt, request);
  emit({
    type: "sidecar",
    event: "started",
    bin: located.bin,
    pid: child.pid ?? null,
    resumed: Boolean(request.resumeSessionId)
  });
  writer?.flush(reducer);
  const releaseStopHandlers = installStopHandlers(child, options.stdinLease, (reason, outcome) => {
    const event = { type: "sidecar", event: "stopping", reason, outcome };
    emit(event);
    record(event);
  });
  const stdoutDone = child.stdout ? forwardLines(child.stdout, (line) => {
    process.stdout.write(line + "\n");
    try {
      record(JSON.parse(line));
    } catch {
    }
  }) : Promise.resolve();
  const stderrDone = child.stderr ? forwardLines(child.stderr, (line) => {
    const event = { type: "sidecar", event: "stderr", data: line };
    emit(event);
    record(event);
  }) : Promise.resolve();
  const exit = await new Promise((resolve) => {
    child.on("error", (e) => {
      const event = { type: "sidecar", event: "error", error: e.message };
      emit(event);
      record(event);
      resolve({ code: 1, signal: null });
    });
    child.on("close", (code, signal) => resolve({ code, signal }));
  });
  await Promise.all([stdoutDone, stderrDone]);
  releaseStopHandlers();
  reducer.finish(exit.code);
  writer?.flush(reducer);
  emit({ type: "sidecar", event: "exit", code: exit.code, signal: exit.signal });
  return exit.code ?? 0;
}
function describe(e) {
  return e instanceof Error ? e.message : String(e);
}

// src/cli.ts
var SUBCOMMANDS = ["probe", "turn", "help"];
function emit2(value) {
  process.stdout.write(JSON.stringify(value) + "\n");
}
async function cmdProbe() {
  const probe = await probeClaude();
  emit2({ type: "probe", ...probe });
  return 0;
}
function cmdHelp() {
  emit2({ type: "help", subcommands: SUBCOMMANDS });
  return 0;
}
async function main() {
  const [subcommand, ...rest] = process.argv.slice(2);
  let code;
  switch (subcommand) {
    case "probe":
      code = await cmdProbe();
      break;
    case "turn": {
      const key = rest[0];
      if (!key) {
        emit2({ type: "error", error: "usage: turn <staged-request-key> [--stdin-lease] [--persist]" });
        code = 1;
        break;
      }
      code = await runTurn(key, {
        stdinLease: rest.includes("--stdin-lease"),
        persist: rest.includes("--persist")
      });
      break;
    }
    case "help":
    case void 0:
      code = cmdHelp();
      break;
    default:
      emit2({ type: "error", error: `unknown subcommand: ${subcommand}`, subcommands: SUBCOMMANDS });
      code = 1;
  }
  process.exitCode = code;
}
main().catch((e) => {
  emit2({ type: "error", error: e instanceof Error ? e.message : String(e) });
  process.exitCode = 1;
});
